<div class="col-md-4">
	<h4><%= @auction.title %></h4>
	<ul id="bids-list">
	<% @auction.bids.each do |bid| %>
		<li><%= bid.supplier.name%> : <%= bid.value %></li>
  <% end %>
	
</div>
<div class="col-md-7">
    <div class="jumbotron all-bids">
    	
	</div>
</div>